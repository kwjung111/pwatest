import { ProgressInfo } from "../LoadingStatus";
export declare function instantiateWebAssembly(importObject: WebAssembly.Imports, wasmURI: string, expectedHash: string, successCallback: (instance: WebAssembly.Instance, wasmModule: WebAssembly.Module) => void, progressCallback?: (info: ProgressInfo) => void): void;
export declare function retryWithExponentialBackoff<T>(handler: () => Promise<T>, backoffMs: number, maxBackoffMs: number, singleTryRejectionCallback: (error: Error) => void): Promise<T>;
export declare class Deferred<T> {
    promise: Promise<T>;
    resolve: (value: T) => void;
    reject: (reason: unknown) => void;
    constructor();
}
